
<header id="header" class="fixed-top header-inner-pages">
    <div class="container d-flex align-items-center justify-content-between">
      <h1 class="logo"><a href="index.html">Protechtini</a></h1>

      <nav id="navbar" class="navbar">
      <ul class="d-flex align-items-center" style="float: right;">
          <li><a class="nav-link scrollto " href="#hero">Home</a></li>
          {% if app.user %}

        <li><a class="nav-link scrollto" href="{{ path('profile') }}">Profil</a></li>
        {% endif %}
            {% if app.user %}
        {% else %}
            {# User is not authenticated - show Login link #}
            <li><a class="getstarted scrollto" href="{{ path('app_login') }}">LOGIN</a></li>
        {% endif %}
        
        <li class="nav-item dropdown pe-3">

        <a class="nav-link nav-profile d-flex align-items-center pe-0" href="#" data-bs-toggle="dropdown">
            <backOffice/img src="assets/backOffice/img/profile-backOffice/img.jpg" alt="Profile" class="rounded-circle">
            {% if app.user %}
            {# Assuming that the user entity has 'getFirstName' and 'getLastName' methods #}
            {% set fullName = app.user.getFirstName() ~ ' ' ~ app.user.getLastName() %}
            <span class="d-none d-md-block dropdown-toggle ps-2">{{ fullName }}</span>
        {% endif %}
        
        </a><!-- End Profile Iamge Icon -->

        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
        <li class="dropdown-header">
        {% if app.user %}
        {# Assuming that the user entity has 'getFirstName', 'getLastName', and 'getRoles' methods #}
        {% set fullName = app.user.getFirstName() ~ ' ' ~ app.user.getLastName() %}
        <h6>{{ fullName }}</h6>
        
        {# Check the user's role and display it accordingly #}
        {% set userRoles = app.user.getRoles() %}
        {% if 'ROLE_SUPER_ADMIN' in userRoles %}
            <span>Super Admin</span>
        {% elseif 'ROLE_ADMIN' in userRoles %}
            <span>Admin</span>
        {% elseif 'ROLE_SOUS_ADMIN' in userRoles %}
            <span>Sous Admin</span>
        {% elseif 'ROLE_EXPERT' in userRoles %}
            <span>Expert</span>
        {% elseif 'ROLE_PHARMACIST' in userRoles %}
            <span>Pharmacist</span>
        {% elseif 'ROLE_DOCTOR' in userRoles %}
            <span>Doctor</span>
        {% elseif 'ROLE_AGENT' in userRoles %}
            <span>Agent</span>
        {% else %}
            <span>User</span>
        {% endif %}
    {% endif %}
    
        </li>
        <li>
            <hr class="dropdown-divider">
        </li>

        <li>
            <a class="dropdown-item d-flex align-items-center" href="users-profile.html">
                <i class="bi bi-person"></i>
                <span>My Profile</span>
            </a>
        </li>
        <li>
            <hr class="dropdown-divider">
        </li>
        <li>
            <a class="dropdown-item d-flex align-items-center" href="{{ path('app_logout') }}">
                <i class="bi bi-box-arrow-right"></i>
                {% if app.user %}
                {# Other user information display code here #}
                
                
                    <span>Sign Out</span>
                
            {% endif %}
            
            </a>
        </li>

    </ul><!-- End Profile Dropdown Items -->

    </li><!-- End Profile Nav -->

                </ul>
        <i class="bi bi-list mobile-nav-toggle"></i>
      </nav><!-- .navbar -->

    </div>
    
  </header><!-- End Header -->
<<<<<<< HEAD
  
=======
  
>>>>>>> 6420834e7355e2da80ba35953ed94643a74ec016
