{% extends 'base2.html.twig' %}

{% block title %}ContratVie{% endblock %}

{% block body %}
    <section class="section register min-vh-100 d-flex flex-column align-items-center justify-content-center py-4">
   <div class="row row g-3">
       <h1>Contrat vie {{ contrat_vie.id }}</h1>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contrat_vie.id }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ contrat_vie.DateDebut ? contrat_vie.DateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ contrat_vie.DateFin ? contrat_vie.DateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ contrat_vie.Description }}</td>
            </tr>
            <tr>
                <th>MatriculeAgent</th>
                <td>{{ contrat_vie.MatriculeAgent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contrat_vie_index') }}">back to list</a>

    <a href="{{ path('app_contrat_vie_edit', {'id': contrat_vie.id}) }}">edit</a>

    {{ include('contrat_vie/_delete_form.html.twig') }}
    </div>
        </section>
{% endblock %}
