<form method="post" action="{{ path('app_vehicle_request_delete', {'id': vehicle_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehicle_request.id) }}">
<button class="btn btn-primary w-100">Delete</button>
</form>
