{% extends 'base.html.twig' %}

{% block title %}SinisterProperty{% endblock %}

{% block body %}
    <h1>SinisterProperty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sinister_property.id }}</td>
            </tr>
            <tr>
                <th>Type_degat</th>
                <td>{{ sinister_property.typeDegat }}</td>
            </tr>
            <tr>
                <th>Description_degat</th>
                <td>{{ sinister_property.descriptionDegat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sinister_property_index') }}">back to list</a>

    <a href="{{ path('app_sinister_property_edit', {'id': sinister_property.id}) }}">edit</a>

    {{ include('sinister_property/_delete_form.html.twig') }}
{% endblock %}
